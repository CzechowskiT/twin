"use client";

import { AuthSiteHeaderBar } from "@/components/auth-site-header-bar";

/** Login/register chrome — logo + account links only; no marketing persona nav. */
export function AuthHeader() {
  return <AuthSiteHeaderBar />;
}
