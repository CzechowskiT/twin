"use client";

import { WorkspaceSiteHeaderBar } from "@/components/workspace-site-header-bar";

/** Authenticated workspace chrome — session nav + persona badge; no marketing arrays. */
export function WorkspaceHeader() {
  return <WorkspaceSiteHeaderBar />;
}
