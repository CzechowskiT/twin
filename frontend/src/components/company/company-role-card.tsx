"use client";
import Link from "next/link"; import { useTranslation } from "@/components/language-provider";
import { type CompanyRole, companyRoleDetailHref, companyRoleQualityPercent } from "@/lib/company-jobs-roles";
function statusClass(s: CompanyRole["status"]){if(s==="active")return"bg-emerald-500/15 text-emerald-300";if(s==="paused")return"bg-amber-500/15 text-amber-300";if(s==="closed")return"bg-zinc-500/15 text-zinc-400";return"bg-sky-500/15 text-sky-300";}
export function CompanyRoleCard({role}:{role:CompanyRole}){const{t}=useTranslation();const q=companyRoleQualityPercent(role);return(<Link href={companyRoleDetailHref(role.id)} className="block rounded-xl border border-[var(--twin-border)] bg-[var(--twin-surface-soft)] p-4 transition hover:border-[var(--twin-accent)]/40"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="text-base font-semibold">{role.title}</h2><p className="twin-muted mt-1 text-sm">{[role.location,role.work_mode?t(`companyJobs.workMode_${role.work_mode}`):null].filter(Boolean).join(" · ")||t("companyJobs.locationUnset")}</p></div><span className={`rounded-full px-2.5 py-0.5 text-xs font-medium ${statusClass(role.status)}`}>{t(`companyJobs.status_${role.status}`)}</span></div><dl className="mt-4 grid grid-cols-2 gap-3 text-sm"><div><dt className="twin-muted text-xs">{t("companyJobs.linkedCandidates")}</dt><dd className="font-medium">{role.linked_candidates_count}</dd></div><div><dt className="twin-muted text-xs">{t("companyJobs.qualityScore")}</dt><dd className="font-medium">{q}%</dd></div></dl></Link>);}
