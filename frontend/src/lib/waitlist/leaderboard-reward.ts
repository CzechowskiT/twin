/** API/demo leaderboard perk — neutral, no cash amounts (matches backend `_leaderboard_reward`). */
export const WAITLIST_LEADERBOARD_REWARD_EN = "Priority founding access";

/** PL-facing label when reward text is shown in UI. */
export const WAITLIST_LEADERBOARD_REWARD_PL = "Priorytet w kolejce founding";
