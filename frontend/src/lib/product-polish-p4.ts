/**
 * Product Polish 1.0 P4 — dashboard declutter, recruiter roadmap, honest social proof.
 * Frontend/UI only — builds on P0–P3 flags.
 */

/** Career compass is pilot/preview — keep off dashboard home (collapsed extended modules only). */
export const SHOW_CAREER_COMPASS_ON_DASHBOARD_HOME = false;

/** Recruiter Daily Cockpit + Trust Review live in collapsed roadmap when primary promos are off. */
export const SHOW_RECRUITER_HUB_ROADMAP_PROMOS_COLLAPSED = true;

/** Mark testimonials, case studies, and homepage social proof as illustrative — not verified. */
export const MARK_ILLUSTRATIVE_SOCIAL_PROOF = true;

/** Footer relabels social proof links with illustrative / founder-led honesty. */
export const FOOTER_SOCIAL_PROOF_ILLUSTRATIVE_LABELS = true;

/** Marquee logo caption removed — keep flag false so guards stay aligned. */
export const SUBTLE_MARQUEE_LOGO_DISCLAIMER = false;
