/**
 * Product Polish 1.0 P5 — company hub cleanup, homepage social proof, launch-perception copy.
 * Frontend/UI only — builds on P0–P4 flags.
 */

/** Hiring cockpit + command center promo cards hidden from company dashboard primary flow. */
export const SHOW_COMPANY_HUB_PRIMARY_PROMOS = false;

/** Cockpit + command center live in collapsed roadmap/pilot details. */
export const SHOW_COMPANY_HUB_ROADMAP_PROMOS_COLLAPSED = true;

/** Calm recommended next action → Roles or Pipeline. */
export const SHOW_COMPANY_HUB_NEXT_ACTION = true;

/** Limit homepage social proof bands — illustrative context, not verified customer proof. */
export const LIMIT_HOMEPAGE_SOCIAL_PROOF_BAND = true;

/** Integrations/ATS surfaces emphasize preview/pilot — not live sync. */
export const INTEGRATIONS_HONEST_NOT_LIVE_SYNC = true;
