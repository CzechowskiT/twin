# Candidate-First analytics

Funnel counters on control plane (zeros until real authorized activity):

`approved_cohort → intake → pack_ready → invited → activated → onboarded`

KPI token stays `NO_REAL_PILOT_DATA` until non-synthetic send + activation. Synthetic smoke never flips KPI. TTV undefined until first real activation.
